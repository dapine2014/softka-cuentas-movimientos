version: '3.1'

services:
  movimientos:
    image: movimiento
    container_name: movimiento
    environment:
      URLDB: jdbc:postgresql://172.20.0.1:5433/sofka
      USER: postgres
      PWD: alex8080
      AUTO: update
      BOOLFORMAT: "true"
      TRACE: trace
      EUREKA_URI: http://172.20.0.1:8761/eureka/
      URI_KAFKA: http://172.20.0.139:29092

    ports:
      - "0:8080"
    networks:
      - data_bases

networks:
  data_bases:
    external: true